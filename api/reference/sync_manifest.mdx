---
title: 'Sync site manifest - returns state hash and signed URLs to blobs (for eventual consistency)  Called by site VMs every ~24h for eventual consistency'
openapi: 'GET /libops.v1.AdminSiteService/SyncManifest'
---
